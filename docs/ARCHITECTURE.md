# Architecture

**Placeholder.** RssReader's concrete architecture (server responsibilities, client(s), storage, sync
strategy, auth model) has not been decided yet — it's the subject of an upcoming roadmap-brainstorming
session. See `docs/ROADMAP.md` once that session has happened.

This file exists now so `AGENTS.md` and `docs/CI_CD.md` have somewhere to point; fill it in with the real
system design once decisions are made.
